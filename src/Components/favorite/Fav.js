import React from "react";

const Fav = () => {
  return (
    <div>
      {/* <h1>{findId.name}</h1>
      <img src={findId.img} alt={findId.name} />
      <FaRegStar className="fav-card" /> */}
      <h1>hi</h1>
    </div>
  );
};

export default Fav;
